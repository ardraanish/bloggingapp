<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header>
            <div class="header">
                <h1>admin pannel</h1>
                <div class="buttons">
                    <button><a href="/login">logout</a></button>
                </div>
             </div>
    </header>

    <div class="userlist">
        <table class="table">
            <thead>
              <tr>
                <th scope="col">user</th>
                <th scope="col">role</th>
                <th scope="col">edit</th>
                <th scope="col">post</th>
              </tr>
            </thead>
            <tbody>
              <% if (locals.user) { %>
               <% user.forEach(e => { %>
                <tr>
                  <td><%= e.username %></td>
                  <td><%= e.role %></td>
                  <td><a href="/admin/user/<%= e._id %>">edit</a></td>
                  <td><a href="/admin/user/post/<%= e._id %>">post</a></td>
                </tr>
               <% }) %>
              <% } %>
            </tbody>
          </table>
    </div>
    </div>
    <!-- <div class="login_area " id="login_area" style="display: none;">
      <h1>user login</h1>

      <div class="login_input">
          <label for="username">User Name</label>
          <input type="text" placeholder="username" name="username" required value="<%= user.username %>"><br>
          <label for="password">role</label>
          <input type="text" placeholder="password" name="role" required>

          
      </div>
      
</div> -->
<script>
// function editUser(){
//   const login_area=document.getElementById("login_area").style.display = "block"
// }
</script>
</body>
</html>